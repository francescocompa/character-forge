{
  "name": "@character-forge/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JSON Schema + shared TypeScript types for the character and session files (T02).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
